{
  "name": "lifenexus",
  "version": "1.0.0",
  "private": true,
  "description": "LIFENEXUS — Personal Knowledge Graph & Memory Engine",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend install && npm --prefix backend run build",
    "start": "node backend/dist/server.js",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "keywords": [
    "lifenexus",
    "personal-knowledge-graph",
    "memory-engine",
    "ai"
  ],
  "author": "LIFENEXUS Team",
  "license": "MIT"
}
